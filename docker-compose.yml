version: "3.8"
services:
  api:
    build: .
    env_file:
      - .env
    environment:
      - TZ=Europe/Madrid
      - PYTHONUNBUFFERED=1
    ports:
      - "8080:8080"
    volumes:
      - .:/app
