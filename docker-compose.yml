version: "3.8"
services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8080:8080"
    environment:
      - TZ=Europe/Madrid
    volumes:
      - .:/app
      - ./timings:/tmp/timings
